# Real Results

Store only outputs produced from real benchmark inputs, real labels, and a recorded run configuration here.

Files in this directory must be traceable to a frozen benchmark manifest, seed, config, command, code revision, Boltz revision, and weights checksum. Synthetic outputs must never be placed here and must never be cited as real.
