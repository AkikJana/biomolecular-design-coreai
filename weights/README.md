# Weights

Model weights are intentionally gitignored and must be fetched or generated separately.

Record the exact source and SHA-256 checksum for any weights used in `data/benchmarks/frozen/manifest.json` and the corresponding experiment config. Do not commit weight files, checkpoints, tensor dumps, or derived binary artifacts to this repository.
